# Initialize the VLLM wrapper (model loads when ``run`` is first called)
vllm = VLLMWrapper(  # loads on first use
# Initialize the VLLM wrapper (model loads lazily when used)
    vllm = VLLMWrapper(  # model loads lazily
# Initialize the vLLM wrapper (loads lazily when ``run`` is called)
    The underlying model weights are loaded lazily when you first call ``run`` or ``batched_run``.
# Initialize vLLM (loads lazily on first ``run``)
# Initialize vLLM (model loads when called)
